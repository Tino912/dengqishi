# ---------------------------------------------------------------- Godot
# 导入与编辑器缓存，Godot 会自己重建。
# 本工程的素材是「运行时加载」（Image.load_from_file），特意绕开了 Godot 的
# 导入管线，所以裸仓库不依赖 .import / .godot/imported 也能跑起来。
/.godot/
godot-lightknight/.godot/
godot-probe/.godot/

# shots/ 是自检的产出目录，而 tools/godot-lightknight.sh 每次都会 `rm -rf` 整个目录重建。
# Godot 只在编辑器扫描 / --import 时才生成 .import，所以这里的 .import **永远追不上** ——
# 跟踪它们等于每次跑完自检都留一堆"已删除"的噪音。
# （assets/ 下的 .import 与素材挨着、内容稳定，因此仍然跟踪。）
godot-lightknight/shots/*.import

# ---------------------------------------------------------------- Node
node_modules/

# ---------------------------------------------------------------- 系统回收站
# 这是桌面环境的回收站目录，不该进版本控制
.Trash-*/
.Trash-0/

# ---------------------------------------------------------------- 杂项
*.tmp
*~
.DS_Store
