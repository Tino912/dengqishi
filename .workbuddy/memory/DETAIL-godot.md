# 灯骑士（dengqishi）· Godot 版细节备忘（MEMORY.md 的展开）
改代码前读这份。另有 skill：`godot-headless-verify`、`git-remote-in-sandbox`。

## 硬约定
1. **`README.md` 是用户给的原稿，不要覆盖**。实现说明写 `IMPLEMENTATION.md` /
   `godot-lightknight/README.md`，原稿最多加一行指引。
2. **交付物必须能被验证**：真跑（真引擎/真浏览器），不接受「应该可以」，固化成一键脚本。
3. **多界面切换的缺陷测不到**（类型检查/构建/纯逻辑仿真覆盖不了），必须真浏览器走完整交互。
4. 断言针对真实状态；累计计数用基线增量，别硬编码。
5. **全绿 ≠ 有意义**：写完断言必须做一轮**变异测试**。
6. **绝对断言与相对断言要配对**：整片"比基准多多少"的断言在基准被改坏时全都不响。
7. **变异"没红"要先分清两种情况**：①补丁没打在生效的那一层；②**断言问错了问题**
   （对该失败方式天然不敏感）。②要**让旋钮转一下** —— 同一帧只改一个变量做前后对照，
   **不是把阈值调松**。本轮实例见下方「夜色 / 迷雾」。

## 一键脚本 / 当前基线（tools/）
- `tools/godot-lightknight.sh` — 完整自检 **390/390**，rc=0，连跑两遍 `report.json`
  **逐字节相同**，基线 md5 `bf6d237cfad4df61f33da242c45808d4`（骨架/姿态动画之后）。**全程约 38 秒**；
  跑两分钟以上通常是卡 400s 超时（脚本没起来），先 grep `SCRIPT ERROR|Parse Error`。
- `tools/godot-mutate.py` — 变异测试 **32 个**，全量约 28 分钟。备份 `~/.cache/dq-mutate/src.bak`。
  ⚠️ **刻意不用 `rmtree`**（沙箱批量删除保护会拦 → 还原失败、坏源码留盘），用
  `copytree(dirs_exist_ok=True)` 覆盖式还原；⚠️ src 一改它就拒绝覆盖备份，先把
  `src.bak` **改名存档**（别删）再重跑。`--list` / 名字子串过滤 / `--restore-only`。
- `tools/godot-probe25d.sh`/`godot-probe.sh`（迁移前遮挡体探针）；
  `tools/browser-verify.sh` + `make-*.py`（Web 版）。

## 本机环境坑
- **沙箱「批量删除保护」**：一次删 >50 文件会被拦（连 `shutil.rmtree` 也拦）→
  还原/同步一律**覆盖式复制**，只写不删。
- 预置 `HTTP_PROXY/HTTPS_PROXY` → 本地回环 502，命令前 unset。
- **`/tmp` 只有 10MB**：`TMPDIR`/`--user-data-dir`/截图输出一律放 `~/.cache/` 下（否则莫名崩）。
- 无头浏览器只有 `google-chrome-stable`；**`--virtual-time-budget` 必崩**；`--dump-dom` 比
  `--screenshot` 稳。Godot `/usr/bin/godot` 4.7.2 + llvmpipe，`DISPLAY=:1`；
  **自检不能 `--headless`**（读不出 SubViewport 像素）。本机无声卡（代码对但听不到）。
- **bash 执行中会持续读脚本**：别边跑边改脚本（会报莫名语法错）；改完 `bash -n` 一下。
  ⚠️ **"边跑边改"包括变异测试在跑的时候** —— `godot-mutate.py` 会反复调用那个自检脚本，
  中途改它会污染整轮结果（本轮踩过一次：只能杀掉重跑）。要改脚本就先停测试。

## Godot 版关键设计（改之前先读）
- 固定步长 `Main.advance(1/60)`，游戏与自检**走同一条代码路径**。
- 输入自己实现边沿检测（`GameInput.just()` + `set_override()`），**不用 `Input.is_action_*`**。
- **四种状态会冻结世界**：`dialogue` / `draft`（**开宝箱也是这个状态**）/ `shop`，
  以及 `World.waves_disabled` 沙盒开关。
  `_pump(...auto_draft/auto_dialogue/auto_shop)` 默认三个都点掉；少点一个 → 后面**成片变红**
  （敌人不动/镜头不跟/灯不亮），极难反查。面板收尾统一 `_resume_play()`。
- **随机源分家**：`_rng`(主仿真) / `_decor_rng` / `_shop_rng` / `_chest_rng` / `_layout_rng` /
  `_elem_rng`。**除 `_rng` 外一律不得走主仿真流**（主仿真流每步消耗由玩家走位决定）。
- **步数是货币**：为取景多 `_pump` 会挪动 RNG 流、撞掉几十条断言；`_shot()` 本身不推世界。
  `_write_png` **自己补 `.png`**（传 `"12-draft.png"` 会落成 `12-draft.png.png`）。
- **三选一两段式**：只有 `Q`/`E` 移高亮、`空格`/`回车` 拿走、`Esc` 放弃；`1/2/3`、`A/D`、
  `J`/左键**全部无效**（用户明确要求）；另有 **0.35s 装填窗口** `Main.DRAFT_ARM`。
- **对比前/后必须冻住其它变量**（半径里有 `combo`、`glow`；`glow` 写成 1.0 后**不再衰减**）。
- 脚下的烘焙光池**不认遮挡**，故意很小；真照明交给会被墙挡的 `PointLight2D`。
- `Bloom`（`BLEND_MODE_ADD`）必须显式 `queue_redraw()`。
- 遮挡体 = **屏幕上画出的整体轮廓（顶面 ∪ 南立面），只削底边 16px**（`LightRig.OCC_TRIM`）。
- 灯贴图必须**竖压 0.62**（= `YSQUASH`），否则地面南北比东西多照 70%。
- Boss 招式池**分阶段**：满血 phase 1 没有 `beam`。
- 关卡 `seed`：L1 10711、L2 20422；自检另钉 `LAYOUT_SEED = 20260925`。
- **rc=1 不等于断言红**：曾 152/152 全绿却每帧打 "Invalid access to property" →
  **必须单独看 `report.json.errors`**。

## 美术 / 素材层
- 素材**运行时加载**（绕开导入管线，裸仓库跑自检不需要 `.import`）；无 mipmap →
  贴图**预先降采样 256×256**。入口 `art.gd`。**程序化为主、贴图只做点缀**（渐变与粒子）。
  许可：Kenney Particle Pack **CC0 1.0**、霞鹜文楷 **OFL-1.1**。
- **HUD 全自绘**（`RBar`/`SkillChip`/`DraftCard`…），不用 `StyleBoxFlat`。HUD 的**暗角后处理**：
  像素采样前必须 `set_post_enabled(false)`；⚠️ 它**管不到全屏 `flash_rect`**（残留闪光会给
  每个采样点加常数，压平比值型断言）。
- Godot 4 易错：`content_margin_*` 属于 `StyleBoxFlat` 不是 `PanelContainer`；内部类引用
  外层常量写 `Hud.C_XXX`；`var _ready := false` 撞 `func _ready()`。

## 道具 / 词条 / 守灯人
- **宝箱**每关 2 个，`E` 开 → 复用三选一面板 → 进**背包**；开过的不复活。
- **背包**只多带一把武器（`prog["bag_weapon"]`）+ 灯油（`prog["shop"]["oil_bank"]`）。
  `X` 换手（背包空时不动）；`C` 喝灯油（回 45%；没药/满血都不消耗）。
- **守灯人**商店：灯油 22 / **重铸** 45（词条推倒重来）/ **锤炼**（加条或升级，全 5 级后
  **不受理也不收钱**）。**8 条词条**全部落在**已有派生属性访问器**上，**别在别处再乘一遍**；
  **挂在「武器 id」上**（`prog["waffix"]`）→ 换手不丢。
- ⚠️ **加道具绝不能直接往 `level["props"]` 里塞**（会挪动 `_decor_rng` → 整关装饰换位 →
  像素断言成片变红）；宝箱走**单独的 `chests` key**、`_decorate()` 之后才入 props。
  **宝箱故意不 solid**（否则插进 `collide_wall` 把动线断言搞崩）。

## 布局随机化（宝箱 / 波次锚点 / Boss 场地每局重摇）
- `Main.run_seed` → `_layout_rng = make_rng(hash([run_seed, level_index, "layout"]))`；
  标题 `new_run()` 换种子，**死亡重开不换**。
- `level["chests"|"waves"|"boss"]` 的坐标**只是数量与兜底值**。宝箱避墙/避出生点/避灯塔/
  箱间距 320；锚点各有净空，**相邻锚点距离卡 460~900**（太近会顺手清掉下一波，太远
  链条被拉断、机器人只清得掉两波）。
- **可达性是最值钱那条**：`blocked()` 只查"点在墙里"，查不出"与玩家之间隔一道墙" →
  那波永远清不掉 → **关卡静默卡死**。用 `has_los` 做**链式**可达（第一波↔出生点，
  之后↔上一波；都对着出生点判会过严到退化兜底）。
  ⚠️ `has_los` 是**约束与断言共用**的函数，写坏了一起被骗；真正的独立证人是
  端到端的「机器人清完了三波」。
- 反直觉：**手写那份布局本身不满足链式可达**，它是按"绕过去"设计的。
- **`waves_disabled` 沙盒开关**：锚点随机后某波可能落进早期段落的活动范围 → 意外刷出 →
  被顺手清掉 → 弹三选一 → 世界冻结 → 几百行后看似无关的断言失败。
  → `Main.waves_off`→`World.waves_disabled` 在 `_update_waves()` **开头早退**；
  自检默认关，只有波次段/第二关段放开并先 `restart_level()`。
  ⚠️ 别在 setup 里标记（会漏掉"清一波弹三选一"，实测 29 条断言被跳过）。

## 元素 / 攻击发光 / 三图
- 元素是**五种不同形状**不是五个倍率：火=灼烧 3.0s/22%；毒=6.0s/10%+减速；冰=冻结 1.15s
  （**连外力速度都清掉**）；雷=麻痹+连锁**一跳**（`has_los` **隔墙不连**）；光=对 `shadow` 系
  ×1.30（**只对暗影系**，是"对策"）。`_elem_rng` 种子 `hash([run_seed,"elem"])`，**不带
  `level_index`** → 挂在武器 id 上（`prog["welem"]`）→ 换手不丢、过关不重摇；允许重复。
  弹丸颜色是**发射那一刻的快照**。攻击发光接进 `LightRig`（挥击灯 `"swing"`、弹丸灯
  `"proj:<pid>"`），**都带遮挡**；**`FX_LIGHT_MAX = 6`**（不封顶会拖垮帧率）。
- 三关：`court` 2400×1700 / `quarry` 2800×2000 / `river` 3000×2200（`lampdemon_shadow`）。
  `Art.body_kind()` 登记表：10 种敌人 → **9 套身子**。断言要写「**除「灯影」本身没有敌人
  落回兜底造型**」，不写"每种画出来不一样"（永远绿）。
- ⚠️ **给敌人的位移别走速度通道**：链锁牵引原是 `e.vx += cos*pw`，被冻结（清速度）和毒减速
  吃掉 → 同一条断言因"这局抽到什么元素"红绿。改成一次位移 `yank` + 清速度。

## 夜色 / 迷雾 / 起步攻击范围（最近一轮）
- **`BASE_REACH := 1.20`**（刀锋 66→79.2），`reach_mul()` 用**加法**（乘系数会把已有的
  "+0.12/级"断言搞失真）。**有两条绝对断言钉住**（`==1.20`、`swing_reach()==79.2`）。
  机器人交手距离从 `swing_reach()` 推（`press_in=rch*0.78`/`atk_at=rch*0.95`），
  不再写死 62/64/44 —— 否则它不会用远程武器，"时而清完三波、时而只清一波"。
- **夜色**：`LightRig` 的 `NIGHT_MIN/SPAN/BASE`；**旧版区间写错**（三关落点全在 `t=0`，
  永远最暗、图纯黑）。三关调色板提亮时**色相没动**，只抬明度。
  ⚠️ **地图提亮会让比值型像素断言失效**（底色 0.17 把 0.07 的光除成 1.4 倍）→
  遮挡那组改成看**增量**（光的贡献本来就是加法）。
- **迷雾 `src/fog.gd`**：**不能用 Light2D**（2D 光照只能加，`BLEND_MODE_SUB` 减 RGB 不减 alpha）。
  做法：屏幕切 **80×45** 格 → 每格算"被照亮多少" → 80×45 贴图 → 全屏着色器画雾 +
  三层倍频噪声雾絮。视线用**射线扇**（192 条/盏 × `Proj.ray_rect_dist` slab 法）。
  `World.step()` 里调（固定步长，雾絮时间取自 `world.time` → 漂动也可复现），每 0.05s 重建；
  有开关 `set_fog_enabled`；`KIND_SCALE` 给每类灯开雾半径系数（玩家 0.82）；
  `MIN_OPENER_R` / `OPENER_MAX=10`；**`MIST_MAX = 0.45`**（用户先后要过"最浓"1.0 与
  "淡一点、让地形可见"，最后量成 0.45 —— 见下面的曲线）。
  清关 `disperse()/reset_fade()/tick_fade()` 走 `World.tick_fx`。
- ⚠️ **雾浓这个数是量出来的，而且"淡一点"必须淡过拐点**。做法：**冻结世界**
  （`main.manual=true` 时 `_process` 直接返回，只有 `advance()` 推步）**+ 同一帧只拧
  `mist_max` 一个 uniform**，所以逐像素只有雾浓一个变量。同一块无 HUD / 无玩家光晕的地面：

  | α | 均值 | 局部对比度(vs 关雾) | **与关雾图的高频相关性** |
  | --- | --- | --- | --- |
  | 0(关雾) | 42.5 | 3.90(100%) | 1.00 |
  | 0.40 | 62.1 | 2.78(71%) | 0.93 |
  | **0.45(现在)** | **64.6** | **2.67(68%)** | **0.91** |
  | 0.60 | 72.0 | 2.38(61%) | 0.79 |
  | 0.70 | 76.9 | 2.28(58%) | 0.68 |
  | 1.00 | 91.2 | 2.21(57%) | **0.23** |

  - **曲线在 0.70 以上是平的**：1.00 → 0.70 只买回 1% 对比度，均值却从 91 掉到 77
    → "淡一点"只淡到 0.70 等于白改；有响应的是 **0.45~0.50**。
  - ⚠️ **指标选错会得出相反结论**：局部对比度会被雾**自己的**絮状纹理抬高
    （α=1.0 还剩 57%，看着像"地形留了一半"，其实全是雾的絮）。
    要问"地形还剩多少"，得拿**雾关那张图的同一块高频分量**做相关：
    1.0 → **0.23**、0.45 → **0.91**。
- **取样手法**（可复用）：临时在 `_section_fog_night()` 里插一段循环取图（探针代码
  `git checkout` 撤掉、不进提交），图另存 `~/.cache/dq-mist/`（下次自检会 `rm -rf shots/`）。
- ⚠️ **迷雾的坐标系 ≠ 灯光层**（真 bug）：`LightRig`/`Bloom` 的 `position` 是
  `cam_offset(...)`（它们的局部空间是"世界坐标投影过来的"），迷雾照抄了这句，
  但它是**屏幕空间** → 矩形被搬出屏幕，**只有 ~53% 屏幕被雾盖住**且是照亮场的错位切片。
  发现方式：**把 fog_on/fog_off 两张截图相减打 ASCII 亮度图**。
- ⚠️ **屏幕空间的滞后状态不能跨相机移动来比**（验回填时 `teleport` 走 → 读到 0，
  其实是"另一格的历史"）→ 改成不搬相机（连击掉光、半径缩回）。
- ⚠️ **整体提亮会让逐像素差异断言退化成恒真**（任何两张图都 ~99% 差异）→
  `_diff_ratio_norm`：先各自减掉自己那块的平均色再比；三图 88/93/98%，
  "同关重建两次"两种度量都 **0.00%**。这类指标要**能自证**（曾因漏跳过越界占位符
  吐出 101.6% 这种不可能的值）。
- ⚠️ **一条"没牙"的断言是靠变异测试才暴露的，而且这个坑在"雾拉满"时又出现了第二次**。
  "夜色"原来只有 `mean > 0.05 / < 0.45`；变异 `夜色永远压到最暗一档`（把 `t` 写死 0）
  **0 条变红** —— 不是补丁打歪了，是**命题问错了问题**：提亮之后最暗一档也只是暗掉一半，
  照样落在区间里（那两条防的是提亮前底色 0.008 的年代）。
  → 补一条**只拧一个变量**的对照：同一帧、同一世界拧 `world.ambient` 到 0.98（`d=0.02 → t≈0`）。
  **第二次**：`MIST_MAX = 1.0` 之后没照到的地方不透明，**全屏均值由雾决定**，
  把地图压暗屏幕几乎不动（0.2925 → 0.2839，只差 **0.0086**，卡在 0.02 阈值下）。
  → 差一点又去调阈值；正确的修法是**关雾再量**（`set_fog_enabled(false)`），
  关雾后 **0.1580 → 0.0986**（差 0.059）。雾开着那个 0.0086 改成只记报告不断言的样本
  `night_delta_fog_on`（它说明"雾拉满后夜色已被雾盖住"，是"最浓"的必然结果）。
  **教训**：断言没红先分清"补丁没打在生效层"还是"断言问错了问题"；后者要**让旋钮转一下**
  （单变量前后对照），**不是把阈值调松**。后半条推广成：**量哪一层，就把别层冻住。**
  `_section_fog_night` 现在 28 条。
- ⚠️ **三关 `ambient` 是 0.90 / 0.955 / 0.975**（`d = 1-ambient` 才进 `NIGHT_MIN/SPAN/BASE`
  的公式）—— 所以 `ambient` 越大越黑，拧到 ~0.98 才到最暗档；直接拿 `ambient` 当"亮度"
  理解会读反。

## 武器挥舞 / 角色骨架 / 施法姿态（上一轮）
- 新增 **`src/pose.gd`**（`class_name Pose`，全是 static 纯函数，**不碰 RNG**）：
  只回答"这一刻身体各部位什么角度"，绘画归 `art.gd`。抽出来的理由是动画有**三条数学命题**
  —— 关节真在转、两腿**严格反相**（`hip + hip_b == 0`）、挥出**角速度剖面**（峰值高 + 末尾急停）。
  混在 `_draw` 里这三条只能靠像素猜。⚠️ 新脚本要跑一次
  `godot --headless --path <proj> --import` 才被全局注册（否则报 `Pose` 未声明）。
- ⚠️ **`Pose.ATTACK_DUR = 0.2` 必须与 `world.gd` 的 `p.attack_t = 0.2` 一致**（分居两文件，
  只改一边画面照样跑）→ 有一条**专门对账**的断言。
- `Art.player()` 重画成骨架，所有部位走**唯一**入口 `_bx(xf, dx, dy)`（原点在脚底，
  一次搞定镜像 + 落地压扁 + 整体旋转）。腿 = 两段 FK（髋→膝→脚，屈膝只在**后摆**半程）；
  斗篷**开衩两片、下摆停在膝上**（y=−14，否则腿画了也看不见）；**手是武器旋转的圆心**
  （上一版武器是沿直线平移 → 就是用户说的"只有一个攻击范围"）。`grip` 逐武器不同
  （杖 −1.42 / 镰 −0.52 / 弩 0…），待机姿态也各不相同。
- **挥击**：前摇 `ease_in_out`（占比 `clamp(wind/0.2, 0.15, 0.55)`）+ 挥出 `ease_out_quint`
  （起步即最高速、**末尾趋零 = 急停**，打击感就在这）。实测（刀）：转折点 0.500 == wind 占比、
  扫过 2.175 == `arc*1.45`、前摇峰值 4.271 / 挥出峰值 **20.689（4.84×）** / 末段 **0.015（0.07%）**。
  `p.attack_alt` 只进 `dir` → 左右开弓。
- **施法姿态**：`Pose.cast_kind_of(id)` **按 id 前缀推**五类（spin/slam/thrust/toss/raise），
  不给 19 个技能加字段。每类三段（起手→放出→收势，收势必须独立一段，`last < 1e-6` 钉住）。
  `CAST_DUR = 0.62`。触发链路纯表现：`use_skill()` 里在 `cast_skill()` 前写
  `p.cast_t` / `p.cast_kind`，`tick` 里递减；**战斗判定一个字没改**。
- ⚠️ **"回身一周"类技能不能绕脚底转**（侧视里转 327° 头会进地板，像摔倒）→
  用**镜像翻转** + 一点侧倾；武器角要自己翻（`wang = PI - ang`，因为 `_bx` 的镜像只作用于位置）。
- ⚠️ **强光会把角色洗成白团**（第一版整片白）。修法：自身光晕 23→**12.5**、α 0.85→**0.5**；
  底色压暗；**每块形状加 `#0b0e16` 描边**。反直觉但成立：**把角色画暗反而更清楚**，
  因为"清楚"来自对比度不是亮度。同一轮"反相两帧腿部像素差"从 0.0077 提到 **0.0233**。
- ⚠️ **手动模式（`main.manual = true`）世界不重绘** → 改 `p.walk_t` 后必须显式
  `w.mark_redraw()`（已封成 `World.mark_redraw()`），否则 8 个相位截出 8 张一模一样的图。
- 姿态段 `_section_pose_anim()` **30 条断言**，四层：对账 2 / 纯数学 11 / 像素 7 / **端到端 2**。
  端到端那两条是这一轮的重点：驱动**真实输入**（按 14 步移动键 → `walk_t` 推进 3.483；
  真调 `use_skill(0)` → `cast_t`/类别由**游戏**设上，45 步后自己走完）。
  **纯逻辑仿真覆盖不了"画到屏幕上没有"；"手动设 cast_t 能画出姿态"也覆盖不了"玩的时候真的会给"。**
- ⚠️ **一条断言被两个独立的层各能单独满足 → 它就是个没牙的烟测**（本轮最大的收获）。
  `★ 挥击的前摇与挥出画出来不一样`：把 art 层武器的 ang/ext/lean 全按回基准值，
  它**照样绿**。两个原因：① `sw["lean"]` 带着躯干/头/斗篷一起动（采样窗 156×96 全框进去）；
  ② `world.gd` 还有一道跟着 `attack_t` 扫的**刀光弧 + 月牙斩**（`sweep = a0+(a1-a0)*t01`）
  落在同一个窗里。→ 修法是**让旋钮转一下**：补一条 `p.attack_alt` 单变量对照
  （alt 只进 `dir`，躯干/腿/斗篷全不变，`attack_t` 不变 → 世界层刀光也冻住）。
  原断言改了措辞、并在变异表里列进 **forbid**（"应该保持绿"= 它的能力边界，不是漏测）。
- ⚠️ **`_region_diff` 的均值会被"窗里有多少空地"稀释**：武器是根细线，在 156×96 窗里
  只占 1~2%，则"只翻挥向"这一对**明明动了**，均值只有 **0.0094**（卡在 0.02 下）。
  → 新增 **`_region_moved(a,b,r,eps,step)`** = "**动了**的采样点占比"（对空地免疫）。
  实测：武器区 **0.1696**、地面区 **0.0000**、同一姿态重取帧 **0.0000**。
  它的盲区是"整片轻微变亮会让所有点都动" → **只用在"同一帧只拧一个变量"处，且必须配 0 对照**。
- ⚠️ 配对照时踩的两个坑：① 第一版的"重取"对照写成 `_region_moved(im_l, im_l, ...)`
  —— **同一张图自己比自己**，恒为 0，什么都没验证；② "腿部区域"这个对照区被挥过来的武器
  切了个角 → 量出 **0.1024** 的假阳性（看着像"腿也在动"）。
  定位靠老办法：**把两帧相减打变化图**，变化像素全落在"相对脚底 x∈[+2,+35]、
  y∈[−48,−9]"这条 34×40 带子里（就是刀）。照包围盒重选对照区就干净了。
- ⚠️⚠️ **一条断言里同时藏了三个错、而且互相掩护**（本轮最耗时间的一处，全量变异跑完才暴露）。
  受害者 `★ 走路的两个反相相位在屏幕上确实不一样`，它以"**额外**变红"出现在
  `夜色永远压到最暗一档` 与 `挥击灯的半径不再跟攻击距离挂钩` 两条**跟走路无关**的变异里：
  1. **「反相两帧」取错帧**：代码写"第 0 与第 4 帧（差半周期）是反相极端"，
     但 `hip = sin(ph)*AMP`，**`sin(0)` 与 `sin(π)` 都是 0** → 两帧都是**中立位**、画得一模一样。
     真正的反相极端是相位 π/2 与 3π/2（第 **2 与第 6** 帧）。
  2. **于是它量到的是 sway / cloak**（腿本来就相同），跟腿没关系。
  3. **采样窗开太大**（y ≥ −26，56×30）把斗篷下摆（最低 y=−13）与躯干底（y=−17.5）
     都框进来 → **把腿冻住也照样有差异**。
  三个错互相掩护："腿冻住打不红它"看着像"它在盯腿"，"地图压暗能打红它"看着像"亮度问题"。
  → 三处一起修：真反相帧（π/2 vs 3π/2）+ 窗收窄到 **y ≥ −12**（里面只有小腿/脚/影子/地板；
  空手臂最低 y≈−20.5、武器 y≈−23，够不进来）+ 判据改**按亮度归一化**
  （`_region_diff_norm` = 逐通道差 / 该区平均亮度）。
  归一化差从 **0.0101**（收窄后暴露的真相）→ **0.0861**（阈值 0.03，2.9 倍余量）。
  并补变异"腿不再跟着姿态画"（只把喂给 `_draw_leg` 的髋角/屈膝换成 0）——
  现在**能打红**了；两条无关变异也不再连坐（`夜色…` 2 条红 → **1** 条）。
  **教训**：① 断言红得莫名时先查它到底在量什么（"取错帧"会让断言恒真/恒假，外面看不出来）；
  ② 采样区**只装你要盯的那一层**；③ 绝对像素差只在"两次同一光照"时可比，
  **要么归一化、要么配相对对照**（绝对差随亮度成比例缩放）。
- 本轮 7 个变异（累计 32）：两腿不再反相 / **腿不再跟着姿态画** / 绘制层无视挥击姿态 /
  `alt` 不进 `dir` / 挥出缓动换 `v*v` / `use_skill` 不写 `cast_t` / `cast_t` 不递减。
  ⚠️ **互为反向的失败方式要各有一条**（"不触发" 与 "触发了收不回来"），
  只测一边的话"卡在施法姿势上"这种最常见的翻车完全没人管。
  ⚠️ 第 1、2 条打在**两个不同的层**（求解器 / 绘制层），第 2 条的 `forbid` 里
  "两条腿严格反相"必须保住 —— 用来证明改坏的确实只有"画"这一层。
- 新增截图：`30-walk-cycle` / `31-swing-arc` / `32-weapon-poses` / `33-skill-poses` / `34-swing-alt`。

## 挥击范围线 == 实际判定 · 伤害数字分级（最近一轮）
- **症状**（用户报的）："一些武器实际攻击范围与标出来的线不符"。
- **根因**：同一次挥击有**两条独立算的几何**。
  - 判定：`w["arc"]` 与 `range * range_mul * reach_mul`；
  - 画线：角度**写死 0.9 rad**、射程还多乘了个 **0.9**。
  于是只有 `arc` 恰好 = 0.9 的武器对得上：**长枪画宽 45%、镰刀画窄 67%**，
  弩（`style == "shot"`，本来没有近战扇形）**也画了一条**出来。
- **修法（唯一真源）**：`World.swing_cone(w, arc_mul, range_mul)` 出几何 ——
  形状 = **半径 `SWING_INNER`(40) 的实心圆 ∪ 内径 40 → 外径 r 的扇形**（"钥匙孔"，
  近身贴脸也要打得到，不是从圆心起算的纯扇形）。
  - 判定 `World.cone_hits(cone, facing, ...)`；
  - 画线 `World.cone_polygon(cone, facing, seg)`（**循环必须从 `i = 0` 起**，
    从 1 起会漏掉内弧在 `a1` 处那个顶点 → 画出来的线比真扇形多一小条，
    逐点比对会挂）。
  - ⚠️ `cone_polygon` 那个 `for i in seg + 1` 的 off-by-one 就是这么抓出来的：
    断言用 `Geometry2D.is_point_in_polygon` 拿**画出来的多边形**当参照系反查 `cone_hits`。
  - ⚠️ `cone_hits` 里读 **参数 `w`** 而不是 `player.weapon()`：整片半径全变成
    79.20（刀锋的值）那次就是这个错 —— 断言侧要用 `Content.WEAPONS[wid]` **独立**算期望值，
    否则是自证同义反复。
  - 谁是近战：`World.has_swing_sector(w)` = `style != "shot"`。⚠️ 它同时也**管着
    抛射物分支**，改坏它会让弩连射都不射（变异表里那条"额外红"是合法的，不是误伤）。
  - 几何存在 **`player.swing_cone`**（不是 world）：普攻 `range_mul=1.0` 与连击
    `range_mul=0.9` 的射程不同，得跟着这一次挥击走。纯表现字段。
- **自检怎么证明"线 == 判定"**（`_section_swing_cone()`，19 条）：
  8 把武器各取扇形内外共 **5040 个点**，`cone_hits` 与 `is_point_in_polygon` **零分歧**；
  再加**沿地面坐标**取样的像素带（投影时按 `Proj.YSQUASH` 压扁，Kronecker 序列抖动
  避开抗锯齿接缝）：外圈 0.0859 / 圈外 0.0000 / 内圆 0.1563 / 重取帧 0.0000。
  另有一个开关 `World.swing_guide_only`：**只画范围线不画刀光**，专门给像素测试用。
- **伤害数字**（用户要求"放大"）：`Art.TEXT_SIZE = {hit:22, crit:28, dot:16, hurt:22,
  heal:17, block:17, status:16}`，`_add_text(...)` 加 `txt_key` 参数从表里取字号
  （10 个调用点全改）。描边偏移随字号缩放（`maxf(1.4, size*0.11)`）。
  **发光改成显式键表** `TEXT_GLOW_KEYS = ["crit","hurt"]` —— 原来是隐含的
  "`size >= 17` 就发光"，一放大就全场发光。渲染实测：同一串「-12」亮像素
  644（22）→ **1130（28），×1.75**。
- ⚠️ **`manual` 模式（世界冻结）下 `slash` 特效会永远卡在 `life = 0.12`**：
  走路 8 帧画廊里凭空多出一道黄色地面弧，而 `attack_t` 明明是 0。
  修法：拍画廊前清 `effects` / `texts` / `particles` + `p.swing_cone = {}`，
  **并补一条"画廊是干净的"断言**（否则下次还会中招）。
- 本轮 8 个变异（累计 40）：半径丢掉 `range_mul` / 绘制层自己重算（0.9 半径 + 写死
  0.9rad）/ 绘制层多画 1.3 倍 / 绘制层丢掉内圆 / 判定忽略角度 / 弧宽写死 0.9 /
  把弩当近战 / `float_text` 无视 `ft["size"]`。
  新增截图：`35-swing-guide`（范围线单独可见的钥匙孔）、`36-text-size`（22 vs 28）。
- ⚠️ **全量跑完 39/40 —— 唯一那个不是断言的问题，是 `expect` 抄错了断言名。**
  「灯弩被当成近战」那条的四条 `expect` 多写了「★ 」前缀，而真实断言名**没有**星号
  （星号只有部分断言有）。子串匹配不报错 → 跑满 27 分钟后显示成"预期变红却没有"，
  看起来像"断言没牙"，其实那四条**正常变红了**（灯弩真的不射箭了）。
  修法：`expect`/`forbid` **一律对着 `shots/report.json` 的 `checks` 键名原样抄**。
- ✅ **给工具加了前置校验**（`unknown_targets()`）：基线绿了之后、开跑任何变异之前，
  先把每条 `expect`/`forbid` 拿去和**全量断言名**对一遍，对不上就立刻 return 1 并
  点名报错。**这道校验自己也验过**：塞一个写错的名字 → 立刻被抓；
  全量 40 个 → 返回空。以后这种抄错 40 秒就被拦下，而不是 27 分钟。
  ⚠️ 顺带一条通用的：**"工具改动"也要有变异/反例验证** ——
  光看到它没报错，说明不了它在盯着东西。

## 仓库 / 版本控制
- `origin` = **`git@github.com:Tino912/dengqishi.git`**（SSH，`/home/tino12/.ssh/id_rsa`）。
  沙箱 push/pull 要绕三层墙 → 见 skill **`git-remote-in-sandbox`**。
- 已加 `.gitignore` 并移出 `.godot/`、`node_modules/`、`.Trash-0/` 的跟踪；
  `shots/` 与 `dist/` **保持跟踪**（给人看的产物）。
- 提交：`7b95646`(基线) → `c5e8ed2` → `64886d7` → `e9c6ba6`(+8 断言) → `9d6d361`(+9) →
  `bb81642` → `0b9e2f8`(元素/攻击发光/第三关，+92，324/324) → 本轮(夜色/迷雾/攻击范围，357/357)。

## 已知待办（未做）
- 设计稿"盲女被吃→化为力量"那一关未做（盲女只在第二关**同行**）；**无升级/存档/传送**。
- **元素没平衡**（倍率是初值）、元素之间**没有组合**、无抗性/免疫。
- **迷雾只是"一层会飘的灰"**：没有风向/体积感/室内外之分，且**纯视觉**
  （敌人躲雾里照样被打中，雾不提供隐蔽或视野代价）。
- 敌人弹道只做了 leech；`spitter` 未与 Web 逐帧比对；无手柄/触屏。
- **Web 版 `render.ts` 的 `applyLighting` 用 `createRadialGradient` 画屏幕正圆**，
  在 2.5D 地面上是椭圆 → 南北多照 60%。**与换引擎无关，可独立先修**（竖压 `YSQUASH`）。
